<div id="<%= dom_id post %>">
  <%= turbo_frame_tag post do %>
    <p>
      <strong>Title:</strong>
      <%= post.title %>
    </p>

    <p>
      <strong>Content:</strong>
      <%= post.content %>
    </p>
    <%= link_to "Edit", edit_post_path(post), target: "_top" %>
  <% end %>
</div>
